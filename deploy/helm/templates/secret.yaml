apiVersion: v1
kind: Secret
metadata:
  name: agent-system-secrets
type: Opaque
data:
  # Replace this with the output of:
  #   echo -n "your-anthropic-key" | base64
  anthropic-api-key: {{ .Values.anthropicApiKey | b64enc | quote }}
